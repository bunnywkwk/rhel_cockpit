---
# Systemd socket activation management

# Enable and start cockpit.socket so Cockpit listens on TCP port 9090 on demand
- name: Enable and start Cockpit systemd socket
  ansible.builtin.systemd_service:
    name: cockpit.socket
    enabled: true
    state: started
